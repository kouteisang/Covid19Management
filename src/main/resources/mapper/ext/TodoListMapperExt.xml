<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.covidmanage.mapper.ext.TodoListMapperExt">
    <resultMap id="BaseResultMap" type="com.covidmanage.pojo.TodoList">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="identity_id" jdbcType="VARCHAR" property="identityId" />
        <result column="todo_thing" jdbcType="VARCHAR" property="todoThing" />
        <result column="is_deleted" jdbcType="INTEGER" property="isDeleted" />
    </resultMap>

    <insert id="addTodoList">
        insert into todo_list(identity_id, todo_thing, is_deleted)
        values(#{identityId}, #{todoThing}, 0)
    </insert>

    <select id="getTodoList" resultType="com.covidmanage.pojo.TodoList">
        select *
        from todo_list
        where identity_id = #{identityId}
        order by id desc
    </select>

    <select id="getTodoListByIdAndIdentityId" resultType="com.covidmanage.pojo.TodoList">
        select *
        from todo_list
        where identity_id = #{identityId} and id = #{id}
    </select>

    <update id="editTodoListStatus">
        update todo_list
        set is_deleted = #{flag}
        where id = #{id} and identity_id = #{identityId}
    </update>

</mapper>